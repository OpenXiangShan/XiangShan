# V2 RTL 内部 Flow 知识索引

## 版本范围

- RTL 版本：V2。
- 版本 profile：`mem_ut/ver/ut/memblock/rule/version/v2`。
- 长期知识目录：`AI_DOC/analysis/rtl/v2/flows`。

本索引只收录经 V2 权威 RTL/Scala 源码核验的内部功能 flow。历史专项分析仍可保留在 `AI_DOC/analysis/rtl`，但新增长期知识统一进入本版本 `flows/`。

## Flow 文档

| Flow 文档 | 关键词 | 覆盖模块 | 入口信号/函数 | 关联 Agent/Flow |
|---|---|---|---|---|
| [Memory flushPipe flow](flows/memory_flush_pipe_flow.md) | `flushPipe`、`sfence.bits.flushPipe`、`sfence.bits.rs1/rs2`、`priv_virt_changed`、`dvirt`、MPRV、MPV、`PTE.G`、`s2xlate`、over-fence、FENCE、SFENCE、HFENCE.GVMA、HFENCE.VVMA、Svinval、`VSETVL`、`NewCSR`、`TLBNonBlock`、DTLB hit/miss、`PTWFilterEntry` flush、`s1_kill/s2_kill`、MissQueue `cancel`、CBO、CMO、`flushAfter`、`s3_flushPipe`、`s3_rep_frm_fetch`、STA0/STA1 | Decode、Fence FU、CSR/NewCSR、MemBlock、TLB、TLBStorage、PTW、LoadUnit、HybridUnit、StoreUnit、StoreQueue、SBuffer、Uncache、DCacheWrapper、LoadPipe、StorePipe、MissQueue、ExceptionGen、ROB | Decode `flushPipe`、Fence `flushSb/sfence.valid`、NewCSR `resetSatp/triggerFrontendChange/status/vstart/frm/dvirt`、`sfence.valid`、`priv_virt_changed`、`robIdx.needFlush`、`deqCanDoCbo`、`mmioStout` | mem_ut sfence flow、memory trigger flow、DCache agent、int writeback agent |
| [Memory trigger flow](flows/memory_trigger_flow.md) | `trigger`、`TriggerAction`、`BreakpointExp=0`、`DebugMode=1`、`Trace=2/3/4`、`None=15`、`tdata`、`triggerCanRaiseBpExp`、LDA/STA/STD | NewCSR Debug、MemBlock、LoadUnit、StoreUnit、StoreQueue、ExceptionGen、ROB | CSR `mem_trigger`、Load/Store S1 vaddr、split writeback trigger | memory flushPipe flow、int writeback agent |
| [V2 StoreMisalignBuffer 跨 16B Store Trigger 元数据传播缺陷](flows/store_misalign_trigger_metadata_propagation.md) | `StoreMisalignBuffer`、`trigger=0`、`TriggerAction.None=15`、`BreakpointExp=0`、`s1_in`、`s1_out`、`misalign_enq`、`splitStoreResp`、`writebackSta_0`、`INT_WB_STA0_TRIGGER_PROVENANCE` | StoreUnit、MemTrigger、StoreMisalignBuffer、MemBlock STA0 mux、ExceptionGen、ROB | StoreUnit `s1_trigger_action`、`misalign_enq.req`、MAB `req.uop.trigger`、MAB `io.writeBack`、STA0 trigger | memory trigger flow、Store TLB hit/retry flow、int writeback agent |
| [LSQ 入队与 Redirect 恢复 flow](flows/lsq_enqueue_redirect_flow.md) | LSQ enqueue、`req.valid`、`needAlloc`、`allocated`、physical LQ/SQ entry、`isVNonsegLoad`、`isVNonsegStore`、`canAccept`、`ldCanAccept`、`sqCanAccept`、`LSQLdEnqWidth`、`LSQStEnqWidth`、`numLoadDp`、`numStoreDp`、`IssueBlockParams.numEnq`、`RenameWidth`、registered credit、redirect、cancel、pointer recovery、`flushPipe`、vector LS fault、segment、FOF、AMO/MOU | NewDispatch、LsqEnqCtrl、LsqWrapper、VirtualLoadQueue、StoreQueue、VMergeBuffer、VSegmentUnit、VfofBuffer、AtomicsUnit、Scheduler、IssueQueue、ROB | `fromRename.fire`、`LsqEnqCtrl.do_enq`、`needAlloc && req.valid`、`RegNext(canAccept)`、`RobPtr.needFlush`、vector `toLsq` feedback | memory flushPipe flow、memory trigger flow、ROB 压缩与后端指令信息流 |
| [V2 正常向量访存 uop 与 flow 拆分](flows/vector_memory_uop_flow_decomposition.md) | `EMUL`、`LMUL`、`EEW`、`SEW`、随机约束、descriptor、`VConfig`、`src_4`、`src_vl`、`vconfigWakeUp`、`numUops`、`vuopIdx`、`numLsElem`、`flowNum`、`flowMask`、`activeNum`、`vecReplayMask`、`splitIdx`、`elemIdx`、`多uop`、`data-uop`、`uop一致性`、`uop_vpu_lastUop`、`uop_vpu_isVleff`、`lastUop`、`vecLastFlow`、`vecExceptionFlag`、`isVleff`、`vleff.v`、FOF、`fix-VL`、FOF 发射顺序、`lqDeqPtr`、`VfofBuffer`、`isVsegls`、`vsegldu`、`vsegstu`、`needAlloc`、physical LQ/SQ entry、`vecArith`、`vset*`、unit-stride、strided、indexed、`VMergeBuffer`、`VLEN`、`vl`、`vstart` | Decode、UopInfoGen、DecodeUnitComp、Rename、NewDispatch、VecMem IQ、VLSplit/VSSplit、VMergeBuffer、VfofBuffer、LsqEnqCtrl、LsqWrapper、VirtualLoadQueue、StoreQueue、MemBlock、VSegmentUnit | `emul = veew - vsew + vlmul`、`issueVldu.flowNum := numLsElem`、`vpu.lastUop := payload.lastUop`、`isVleff := isFof && NF==0`、`isVleff && lastUop`、`isVleff && (lqIdx == lqDeqPtr)`、`lastUop -> vecLastFlow`、`isVsegls`、`needAlloc && req.valid`、`GenRealFlowNum`、`GenFlowMask`、`activeNum`、`vstuIqFeedback.feedbackSlow`、merge `flowNum` 归零 | Vector issue agent、LSQ 入队与 Redirect 恢复 flow、ROB 压缩与后端指令信息流、Memory flushPipe flow |
| [V2 RVV 向量接口与执行流综合知识](flows/vector_rtl_comprehensive_knowledge.md) | RVV `v0-v31`、`VLEN`、`ELEN`、`vtype`、`vsew`、`vlmul`、`vma`、`vta`、`vill`、`vl`、`vstart`、`vxrm`、`vxsat`、`vcsr`、`vlenb`、`mstatus.VS`、`issueVldu_0/1`、`enqLsq`、`needAlloc`、`LsqEnqCtrl`、随机约束、descriptor、`src_0~src_4`、`VConfig`、`EMUL`、`LMUL`、`EEW`、`SEW`、`NFIELDS`、`numUops`、`flowNum`、`flowMask`、`activeNum`、`pdest`、`lqIdx/sqIdx`、FOF、segment、replay、writeback | Decode、Rename、NewDispatch、VecMem IQ、MemBlock、VLSplit/VSSplit、VMergeBuffer、VSegmentUnit、VfofBuffer、LsqEnqCtrl、LQ/SQ、OOO vector interface | `enqLsq.req.valid`、`needAlloc`、`src_4[7:0]`、`emul = veew - vsew + vlmul`、`GenRealFlowNum`、`issueVldu.flowNum = numLsElem`、`GenFlowMask`、`activeNum`、`vstuIqFeedback.feedbackSlow`、FOF/segment 路由和 writeback 仲裁 | Vector issue agent、V2 正常向量访存 flow、LSQ 入队与 Redirect 恢复、ROB 压缩与后端指令信息流 |
| [ROB 压缩与后端指令信息流](flows/rob_compress_and_backend_instruction_flow.md) | ROB、RAB、`RobEntryBundle`、`canRobCompress`、`instrSize`、`realDestSize`、`uopNum`、`robIdx`、`replayInst`、`matchInvalid`、`has_exception`、`commitType`、`exceptionHappen`、`RegEnable`、`isStoreException`、fault store、scalar load fault、scalar MMIO fault、early CBO fault、vector load/store fault、FOF、AMO、MOU、AtomicsUnit、`wline`、`flush`、`flushAfter`、`pendingPtr`、`pendingst`、`pendingMMIOld`、`scommit`、`committed`、`completed`、`lqDeq`、`sqDeq`、`sqCancelCnt`、uncache、MMIO fault | Decode、Rename、CompressUnit、NewDispatch、HybridUnit、ROB、RAB、ExceptionGen、IssueQueue、LSQ、VirtualLoadQueue、StoreQueue、VMergeBuffer、VSegmentUnit、AtomicsUnit、SBuffer、Uncache、MemBlock writeback | Decode `canRobCompress`、Rename `robIdx` 分配、HybridUnit `s3_rep_frm_fetch`、vector `toLsq` feedback、atomic writeback、ROB `exceptionHappen/commitType`、`isStoreException`、ROB exception redirect、StoreQueue request/completion/deq/cancel | memory flushPipe flow、memory trigger flow、LSQ 入队与 Redirect 恢复 flow、int writeback agent |
| [V2 StoreQueue NC 跨 16B 异常、读指针与 Redirect 恢复设计确认](flows/storequeue_nc_cross16_exception_rdataptr_redirect_design_confirmation.md) | PBMT NC、`unaligned`、`cross16Byte`、`StoreMisalignBuffer`、`storeAddrMisaligned`、`DataBuffer`、`ncStall`、`sqNeedDeq`、`readyReadGoVec`、`sqReadCnt`、`rdataPtrExt`、`completed`、`sqCancelCnt`、`RedirectLevel.flush`、slot reuse、饥饿、顺序阻塞 | StoreUnit、StoreMisalignBuffer、MemBlock、StoreQueue、DatamoduleResultBuffer、SBuffer、ExceptionGen、ROB | `Pbmt.isNC`、`s1_toMisalignBufferValid`、MAB `isUncache/writeBack`、`ncState`、`misalignToDataBufferValid`、`readyReadGoVec`、`needCancel`、`rdataPtrExt` | Store TLB 命中后的 Replay 与 Retry flow、ROB 压缩与后端指令信息流、LSQ 入队与 Redirect 恢复 flow |
| [Memory PMP/PMA 权限检查 flow](flows/memory_pmp_pma_permission_flow.md) | `tlbCsr_priv_debug`、`priv.debug`、PMP、PMA、`debugStart`、`debugEnd`、access fault、MMIO、atomic | NewCSR、MemBlock、Frontend、L2TLB、PMPChecker、PMA checker | `io.tlb.debug`、`PMPCheckerEnv.debug`、`pmp_match_res`、`pma_match_res` | memory trigger flow、memory flushPipe flow |
| [DCache-L2 refill hint 与 L2 flush done flow](flows/dcache_l2_refill_hint_and_flush_done_flow.md) | `io_l2_hint_valid`、`sourceId`、`isKeyword`、GrantData、MSHR、critical beat、`io_l2_flush_done`、`mflushpwr` | CoupledL2、CustomL1Hint、GrantBuffer、L2Top、DCache MissQueue、LoadQueueReplay、MemBlock、NewCSR | DCache Acquire echo、L2 MainPipe s1/s3、`l2Flush` | LSQ replay flow、低功耗 flow、L2 sideband agent |
| [L2 内侧 TileLink 请求、权限与回复 flow](flows/l2_inner_tilelink_request_response_flow.md) | `AcquireBlock`、`AcquirePerm`、`Grant`、`GrantData`、`AccessAckData`、`CBOAck`、`NtoB`、`NtoT`、`BtoT`、source range、sink、GrantAck、`AliasKey`、`user.alias`、`cache_alias`、`aliasTask`、`B.data[2:1]`、`IsKeywordKey`、`echo.isKeyword`、`corrupt`、`TLError`、`L1ErrorMetaArray`、ICache、PTW、Uncache | DCache、ICache、L2TLB、Uncache、L2Top、CoupledL2 MainPipe/MSHR/GrantBuffer/SourceB、WritebackQueue | L1 A/C/E channel、`odOpGen()`、`ClientMetadata.onAccess()`、CMOUnit、DCache/L2 alias Probe、keyword A/D/hint 路径、corrupt line 聚合 | DCache-L2 refill hint 与 L2 flush done flow、L2 sideband responder |
| [DTLB-L2TLB 多请求与 Response 次序 flow](flows/dtlb_l2tlb_request_response_ordering_flow.md) | `PTWNewFilter`、`PTWFilterEntry`、`PtwReq`、`PtwRespS2`、multi-outstanding、response ordering、raw hit、UID multicast、CSR history、C-2、`mergeArb`、sfence、C4 due、`ptwResp_valid`、barrier | MemBlock DTLB、PTWNewFilter、L2TLB、PtwCache、PTW、LLPTW | `dtlbRepeater.io.ptw.req(0).fire`、`io.tlb(1).resp.fire`、`PTWNewFilter` flush | L2TLB agent、memory flushPipe flow、PMP/PMA flow |
| [Store TLB 命中后的 Replay 与 Retry flow](flows/store_tlb_hit_replay_and_retry_flow.md) | StoreUnit、STA IQ replay、`s2_misalignNeedReplay`、misalign buffer、`feedbackSlow`、`store_replay_resp`、SBuffer、MissQueue、WBQ、B-to-T | StoreUnit、StoreMisalignBuffer、IssueQueue、SBuffer、DCache StorePipe/MainPipe | StoreUnit `feedbackSlow.hit`、MAB `need_rep`、DCache `store_replay_resp` | DTLB-L2TLB、PMP/PMA、trigger、flushPipe、DCache/L2 flow |
| [MMU GPF/AF 异常优先级与并发边界 flow](flows/mmu_gpf_af_exception_priority_flow.md) | `gpf`、`gaf`、`af`、PF+AF、raw `exceptionVec`、`pmpDefault`、U 态、`g_perm`、`g_perm.g`、S2 PTE.G、`level`、`levelNext`、`getVpnn`、Sv39、Sv48、`PtwCache`、`PtwEntry`、`PTWEntries`、`cacheline`、`sector`、`blockBytes_align`、`sel_pte`、`l3Hit`、`l3Refill`、`noS2xlate`、`allStage`、`onlyStage1`、`onlyStage2`、VS-stage、G-stage、PLRU、`perm_check`、`HPTW`、`PTW`、`LLPTW`、`PtwRespS2`、`hasPf`、异常优先级 | PtwCache、HPTW、PTW、LLPTW、L2TLB、L1 TLB、PMPChecker、LoadUnit、StoreUnit、HybridUnit、ROB/CSR | TLB request、PMP checker、64-byte page-table Get、PtwCache request/refill、PTW/HPTW request fire、`HptwResp.apply`、`TLB.perm_check`、`TlbResp.excp`、`ExceptionNO.priorities` | DTLB-L2TLB 多请求与 Response 次序 flow、memory flushPipe flow、PMP/PMA flow、L2TLB agent |

## 维护规则

向量访存 flow 还覆盖 `vecWen`、`v0Wen`、`vlWen`、`ldest`、`pdest` 的 Decode 派生和
writeback 目的寄存器类别约束。

- 新分析优先合并到已有 flow；新增 flow 后必须补充上表。
- 表中关键词应包含用户可能搜索的字段原名和行为同义词。
- V3 差异链接到 `../v3/index.md` 或对应 V3 flow，不在本文混写 V3 事实。

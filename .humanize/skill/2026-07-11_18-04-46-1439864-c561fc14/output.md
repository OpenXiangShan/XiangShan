当前会话无法完成这 12 份文档的只读审查，因为本地文件读取工具在启动阶段就失败了；我没有读取到任何目标文档内容，也没有编辑任何文件或评论其它脏改动。

阻塞点是统一的执行沙箱错误：
```text
bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted
```

这导致 `exec_command` 无法打开工作区文件，所以我不能负责任地给出你要求的“精确文件:行号 + 修复建议”结论，也不能输出 `REVIEW_PASS`。

如果你要我继续，有两种可行方式：
1. 重新提供一个可读取本地文件的会话后，我按当前要求直接审。
2. 你把这 12 份目标文档和 1 份权威参考文档内容贴出来，我基于文本做逐行审查。

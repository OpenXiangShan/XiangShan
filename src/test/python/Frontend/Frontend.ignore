# Frontend line coverage waive file.
#
# Consumers:
# - toffee_test.reporter.set_line_coverage reads this as a .ignore pattern file.
# - scripts/gen_coverage_html.sh maps file-level patterns to genhtml --exclude.
#
# Waive policy:
# - Keep frontend functional RTL visible in coverage reports.
# - Waive only reviewed non-functional support logic.

# MBIST/DFT support logic.
*/build-frontend/rtl/Mbist*.sv
